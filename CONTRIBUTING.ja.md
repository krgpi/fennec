# Fennec へのコントリビュート

English version: [CONTRIBUTING.md](CONTRIBUTING.md)

Fennec に関心をお寄せいただきありがとうございます。バグ報告、機能提案、プルリクエストのいずれも歓迎します。

## コントリビューターライセンス契約（CLA）

**最初のプルリクエストをマージするには、[コントリビューターライセンス契約](CLA.md)への署名が必要です。**

事前の手続きは不要です。最初の PR を作成するとボットが手順をコメントします。署名はコメント1つで完了し、以降のすべてのコントリビューションに適用されます。

### なぜ必要か

Fennec のコアは MIT ライセンスであり、今後もそれを維持します。CLA はコントリビューションの著作権をメンテナに譲渡するもので、コードベースの著作権を一元的に保有し続けるためのものです。これにより、将来の商用版の提供、将来バージョンのライセンス調整、プロジェクトの権利行使が可能になります。著作権が多数のコントリビューターに分散すると、全員を探し出して同意を得ない限りこれらは実行できなくなります。

CLA 第5条により、あなた自身のコントリビューションに対する権利は無制限に留保されます。商用利用を含め、他のプロジェクトで自由に再利用できます。著作者のクレジットは git の履歴に残ります。

署名を希望されない場合も問題ありません。その場合もぜひ Issue で問題やアイデアを共有してください。詳細なバグ報告はそれ自体が十分に有用です。

## バグ報告

Issue を作成し、以下を含めてください。

- OS とそのバージョン、Fennec のバージョン（設定画面に表示されます）
- 使用していた文字起こしエンジン（Apple / Whisper）とモデル
- 期待した動作と実際の動作
- 関連するログ出力

Fennec は音声をローカルで処理するため、**ログに録音内容の断片が含まれることがあります**。貼り付ける前に内容を確認してください。

## プルリクエスト

まず開発環境を構築してください。README の[開発](README.ja.md#開発)を参照してください。

PR を作成する前に:

```bash
cargo fmt --all
cargo clippy --workspace --all-targets
cargo test -p fennec-core
pnpm build
```

以下は知っておくと役立つ点です。

- OS 依存のコードはクレート内に閉じ込めてください。`src-tauri` に `cfg` を漏らさない方針です（[構成](README.ja.md#構成)を参照）
- UI 文言は日本語がキーで `src/i18n/{ja,en}.json` にあります。新しいキーは**必ず両方**に追記してください
- macOS では、録音に関わる変更は必ずバンドル済みの `.app` で検証してください（`pnpm tauri build --debug --bundles app`）。`pnpm tauri dev` は非バンドルバイナリを実行するため、TCC が音声キャプチャを無言で拒否し、録音が無音になります
- diff は小さく保ってください。無関係な整形はレビューを困難にします

大きな変更については、着手前に Issue で方針を相談してください。

## プライバシー

Fennec の中核的な約束は、ユーザーが明示的に要求しない限り音声が端末外に出ないことです。ネットワーク通信、テレメトリ、クラッシュレポートを追加する変更は、どんなに小さくても事前に Issue で議論が必要です。
